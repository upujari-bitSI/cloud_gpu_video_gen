# Core LLM
anthropic>=0.40.0
openai>=1.50.0
python-dotenv>=1.0.0

# Image generation
# Compatibility notes (verified working set on RunPod, April 2026):
#   - diffusers 0.27.x imports cached_download (removed in huggingface_hub>=0.26)
#   - diffusers >=0.30 imports UMT5EncoderModel/AuraFlow (needs newer transformers)
#   - diffusers >=0.31 uses string-form forward refs in @custom_op (breaks torch <2.4)
#   - transformers 5.x removed APIs that diffusers 0.28-0.30 rely on
#   - torchvision must match installed torch ABI (upgrade together)
torch>=2.4.0
torchvision>=0.19.0
diffusers>=0.28.0,<0.30.0
huggingface_hub>=0.23.0,<2.0.0
transformers>=4.40.0,<5.0.0
accelerate>=0.30.0,<2.0.0
safetensors>=0.4.0
xformers; platform_system != "Darwin"

# Audio / TTS
TTS>=0.22.0
elevenlabs>=1.5.0

# Video processing
moviepy==1.0.3
imageio>=2.34.0
imageio-ffmpeg>=0.4.9

# Utilities
Pillow>=10.0.0
numpy>=1.24.0
pydub>=0.25.0

# Optional UI
streamlit>=1.32.0
